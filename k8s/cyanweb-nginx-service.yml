apiVersion: v1
kind: Service
metadata:
  name: cyanweb-nginx
  namespace: ${KUBECTL_NAMESPACE}
spec:
  # type: NodePort
  type: ClusterIP
  selector:
    app: cyanweb-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
      # nodePort: 30001
