apiVersion: v1
kind: Service
metadata:
  name: cyanweb-nginx
  namespace: cyanweb-kube
spec:
  type: NodePort
  selector:
    app: cyanweb-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
